# n8n webhook: POST JSON { "text": "..." } → returns ParseResult (parsed, unparsed, deliveryInstructions, notes, clientName, clientCompany, dynamicColumns?).
# Optional: same URL in VITE_PARSE_API_URL (alias) if you prefer a generic name.
VITE_N8N_PARSE_WEBHOOK_URL=https://your-subdomain.app.n8n.cloud/webhook/your-path
# VITE_PARSE_API_URL=https://... (optional alias; same as webhook URL)
